apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-project-mapping
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /test-project/
            pathType: Prefix
            backend:
              service:
                name: test-project
                port:
                  number: 80

